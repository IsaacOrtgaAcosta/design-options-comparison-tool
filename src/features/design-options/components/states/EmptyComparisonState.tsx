import { CardDescription } from "@/components/ui/card";

export const EmptyComparisonState = () => {
  return (
    <CardDescription className="max-w-md text-base">
      Select more than one option, up to four, and press Compare to review them
      side by side.
    </CardDescription>
  );
};
